# HestAI-MCP Environment Configuration
# Copy this file to .env and add your actual values
# API keys can also be stored in system keyring (more secure)

# =============================================================================
# API KEYS (Required - at least one provider)
# =============================================================================

# OpenRouter - Recommended: One key, access to many models
# Get your key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-your-key-here

# Direct provider keys (alternative to OpenRouter)
# OPENAI_API_KEY=sk-your-openai-key
# Note: Anthropic models are available via OpenRouter (e.g., anthropic/claude-3.5-sonnet)
# Direct Anthropic API is not currently supported as a provider.

# =============================================================================
# DEFAULT AI SETTINGS (Optional - overrides if no ai.yaml exists)
# =============================================================================

# Default provider: openrouter or openai
# Note: Use openrouter to access Anthropic models (e.g., anthropic/claude-3.5-sonnet)
# HESTAI_AI_PROVIDER=openrouter

# Default model for synthesis tier (fast, cheap)
# HESTAI_AI_MODEL=google/gemini-2.0-flash-lite

# Model for analysis tier (balanced)
# HESTAI_AI_MODEL_ANALYSIS=anthropic/claude-3.5-sonnet

# Model for critical tier (best available)
# HESTAI_AI_MODEL_CRITICAL=anthropic/claude-3.5-sonnet

# =============================================================================
# For more control, create ~/.hestai/config/ai.yaml
# See config/ai.yaml.example for the full tiered configuration format
# =============================================================================
